# Project Memory

## Core
Projeto: MT 24horas express (marketplace de delivery multi-loja, PT-BR, mobile-first).
Backend: Supabase EXTERNO (já existente) — nunca sugerir Lovable Cloud. Placeholders em src/lib/supabase.ts.
Stack: TanStack Start (file-based routing em src/routes/) — usuário queria react-router-dom mas template proíbe; mantive TanStack.
Roles em tabela user_roles separada com has_role() SECURITY DEFINER. NUNCA armazenar role em profiles.
Customers são auto-provisionados on-demand no checkout (lazy), não no signup.
Orders RLS: policies PERMISSIVE por comando — nunca RESTRICTIVE FOR ALL (bloqueia cliente).
Audit log tolerante a falhas (PGRST205 → console.info, nunca quebra checkout).
Idempotência em orders via coluna idempotency_key UNIQUE; em 23505 recuperar pedido existente.
Design: cantos rounded-2xl, sombras suaves, tokens oklch em src/styles.css, Plus Jakarta Sans + Inter (sem Poppins).
